function sendDispatch(type)
    if type == "apply" then -- when someone is applying a graffiti
        -- add your own dispatch event/export here:
        
        return
    end
    if type == "removing" then -- when someone's removing a graffiti
        -- add your own dispatch event/export here:

        return
    end
    if type == "robbing" then -- triggered when someone is robbing a gang NPC
        -- add your own dispatch event/export here:

        return
    end
end