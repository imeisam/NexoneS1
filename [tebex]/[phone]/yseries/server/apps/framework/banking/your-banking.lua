if Config.Companies.Banking ~= 'customized' then return end

function GetAccountMoney(accountName)
    -- Implement the function to get the account money
    return nil
end

function AddAccountMoney(accountName, amount)
    -- Implement the function to add money to the account
    return false
end

function RemoveAccountMoney(accountName, amount)
    -- Implement the function to remove money from the account
    return false
end

function AddTransaction(sender, recipient, amount, description)
end
